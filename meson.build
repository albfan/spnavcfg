project('spnavcfg', 'c',
  version : '0.1',
  default_options : ['warning_level=3'])

pkgdatadir = get_option('prefix') / get_option('datadir') / meson.project_name()

subdir('ui')
subdir('icons')
subdir('src')
